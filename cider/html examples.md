This is because im stupid and for some reason cant remember this so instead of googling it i can just come here.
make http requst<br>
`<button class="request-button" onclick="fetch('/your-route', { method: 'GET' })">Send HTTP Request</button>`
